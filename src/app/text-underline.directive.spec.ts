import { TextUnderlineDirective } from './text-underline.directive';

describe('TextUnderlineDirective', () => {
  it('should create an instance', () => {
    const directive = new TextUnderlineDirective();
    expect(directive).toBeTruthy();
  });
});
